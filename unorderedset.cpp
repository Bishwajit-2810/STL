#include <bits/stdc++.h>
using namespace std;

//  it's a c++ container

void explain_unordered_set()
{
    // lower_bound and upper_bound function does not work
    // stores in random order
    // time complixity is O(1) in really worst case in a billion O(N)
    cout << "rest are same as set" << endl;
}

int main()
{

    explain_unordered_set();
}