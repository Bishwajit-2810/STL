#include <bits/stdc++.h>
using namespace std;

void explain_multimap()
{
    multimap<int,int> mpp;
    //  it's a c++ container
    // in sorted order of keys
    //  duplicate keys are possible
}

void explain_unordered_map()
{
    unordered_map<int,int> mpp;
    // in unsorted order of keys
    //  duplicate keys are not possible
    // time complixity is O(1) in really worst case in a billion O(N)
}

int main(){

cout<<"operations are same as map"<<endl;

}